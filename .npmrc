# pnpm workspace configuration
# https://pnpm.io/npmrc

# Disable shameful hoisting (security best practice)
shamefully-hoist=false

# Only hoist ESLint/Prettier plugins needed for IDE integrations
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
