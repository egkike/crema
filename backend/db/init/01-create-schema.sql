-- 01-create-schema.sql
-- No creamos schema custom: usamos el schema por defecto 'public'
-- Esto simplifica el template y evita nombres hardcodeados innecesarios

-- Opcional: aseguramos que el search_path incluya public (por defecto ya lo hace)
SET search_path TO public;